# p4pa-pu-fe

## Adding a module to the project
Adding a module, can be accomplished by adding a directory inside `src/extra` containing a route and sidebar object.
Translations can be added using the i18n-next addResourceBundle method.

